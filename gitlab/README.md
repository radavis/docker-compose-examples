# gitlab-ce

```bash
$ mkdir gitlab-home
$ cp .env.example .env
$ dc up
$ open http://localhost:8080
```

- [GitLab Docs: Docker](https://docs.gitlab.com/19.2/install/docker/installation/#install-gitlab-by-using-docker-compose)
